extends ../layout/index

block contenido 
    .py-10 
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2.text-center.text-6xl.font-extrabold= pagina

    div(class="bg-red shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-green-300').py-2.px-5.rounded-lg.w-full.font-bold.text-center= mensaje

        if !error
            a(href="/auth/login").block.text-center.text-black.bg-white-600.mt-10.py-2.px-8.rounded-lg.w-full.font-bold Ya puedes iniciar Sesión